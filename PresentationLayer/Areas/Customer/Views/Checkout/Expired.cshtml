@{
    ViewData["Title"] = "Payment Expired";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<head>
    <link rel="stylesheet" href="~/assets/Admin/vendors/mdi/css/materialdesignicons.min.css" />
    <style>
        :root {
            --primary-color: #3d5ee1;
            --secondary-color: #2c3e50;
            --dark-color: #1a1d20;
            --light-color: #f8f9fa;
            --accent-red: #dc3545;
            --accent-green: #28a745;
            --accent-gold: #ffd700;
            --accent-orange: #fd7e14;
            --card-bg: #2d3748;
            --border-color: #4a5568;
            --vip-color: #ffd700;
            --cat1-color: #dc3545;
            --cat2-color: #fd7e14;
            --cat3-color: #20c997;
        }

        .expired-section {
            background: linear-gradient(rgba(0, 0, 0, 0.85), rgba(0, 0, 0, 0.85)), url('https://images.unsplash.com/photo-1575361204480-aadea25e6e68?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1920&q=80') no-repeat center center;
            background-size: cover;
            min-height: 100vh;
            padding: 60px 0;
            display: flex;
            align-items: center;
        }

        .page-title {
            color: white;
            text-align: center;
            margin-bottom: 40px;
            font-weight: 700;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            position: relative;
            padding-bottom: 15px;
        }

            .page-title::after {
                content: '';
                position: absolute;
                bottom: 0;
                left: 50%;
                transform: translateX(-50%);
                width: 100px;
                height: 3px;
                background: var(--accent-orange);
            }

        .expired-container {
            max-width: 800px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .expired-card {
            background: linear-gradient(145deg, var(--card-bg), #1a202c);
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
            border: 1px solid var(--border-color);
            padding: 40px;
            text-align: center;
        }

        .expired-icon {
            width: 80px;
            height: 80px;
            background: var(--accent-orange);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 25px;
            font-size: 40px;
            color: white;
        }

        .expired-message {
            color: white;
            font-size: 24px;
            margin-bottom: 20px;
            font-weight: 600;
        }

        .expired-details {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 10px;
            padding: 20px;
            margin: 25px 0;
            text-align: center;
            border: 1px solid var(--border-color);
        }

        .countdown {
            font-size: 18px;
            color: var(--accent-gold);
            font-weight: 600;
            margin: 15px 0;
        }

        .action-buttons {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 30px;
            flex-wrap: wrap;
        }

        .btn-new {
            background: linear-gradient(135deg, var(--primary-color), #2a4bcc);
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 8px;
            font-weight: 600;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            transition: all 0.3s;
        }

            .btn-new:hover {
                background: linear-gradient(135deg, #2a4bcc, #1e3bb8);
                color: white;
                transform: translateY(-2px);
            }

        .btn-home {
            background: rgba(255, 255, 255, 0.1);
            color: white;
            border: 1px solid var(--border-color);
            padding: 12px 25px;
            border-radius: 8px;
            font-weight: 600;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            transition: all 0.3s;
        }

            .btn-home:hover {
                background: rgba(255, 255, 255, 0.2);
                color: white;
                transform: translateY(-2px);
            }

        .ticket-info {
            margin-top: 30px;
            text-align: center;
            color: #a0aec0;
            font-size: 14px;
        }

        @@media (max-width: 768px) {
            .expired-card {
                padding: 25px;
            }

            .action-buttons {
                flex-direction: column;
            }

            .btn-new, .btn-home {
                width: 100%;
                justify-content: center;
            }
        }

        @@keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .expired-card {
            animation: fadeIn 0.6s ease-out;
        }

        .blink {
            animation: blink 2s infinite;
        }

        @@keyframes blink {
            0% {
                opacity: 1;
            }

            50% {
                opacity: 0.5;
            }

            100% {
                opacity: 1;
            }
        }
    </style>
</head>

<section class="expired-section">
    <div class="container">
        <div class="expired-container">
            <h1 class="page-title">Payment Session Expired</h1>

            <div class="expired-card">
                <div class="expired-icon">
                    <i class="mdi mdi-clock-alert"></i>
                </div>

                <h2 class="expired-message">Payment Time Has Expired</h2>
                <p style="color: #a0aec0; margin-bottom: 25px;">Your payment session has expired due to inactivity. No charges have been applied to your account.</p>

                <div class="expired-details">
                    <p style="color: #a0aec0;">The reserved tickets have been released and are now available for other customers.</p>
                    <div class="countdown blink">
                        <i class="mdi mdi-timer-sand"></i> Session expired
                    </div>
                    <p style="color: #a0aec0; margin-top: 10px;">You can start a new booking process if you still wish to purchase tickets.</p>
                </div>

                <div class="action-buttons">
                    <a asp-area="Customer" asp-controller="Match" asp-action="Index" class="btn-new">
                        <i class="mdi mdi-ticket me-2"></i> Find New Tickets
                    </a>
                    <a asp-area="Customer" asp-controller="Home" asp-action="Index" class="btn-home">
                        <i class="mdi mdi-home me-2"></i> Back to Home
                    </a>
                </div>

                <div class="ticket-info">
                    <p>For assistance, please contact our support team.</p>
                </div>
            </div>
        </div>
    </div>
</section>